<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Reset Password</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <style>
    :root{--bg:#0d1117;--card:#111827;--muted:#9ca3af;--accent:#16a34a}
    body{background:var(--bg);min-height:100vh;display:flex;align-items:center;justify-content:center;padding:18px;color:#fff;font-family:system-ui,sans-serif}
    .card-reset{width:100%;max-width:520px;background:var(--card);padding:34px;border-radius:14px;border:1px solid #1f2937;box-shadow:0 24px 60px rgba(0,0,0,.6)}
    h2{font-weight:600;margin-bottom:6px;text-align:center}
    .lead{color:var(--muted);text-align:center;margin-bottom:18px}
    .form-control{background:#0d1117;color:#fff;border:1px solid #374151;border-radius:10px;padding:12px}
    .btn-success{background:var(--accent);border:none;border-radius:10px;padding:11px 14px}
    .password-guideline{font-size:13px;color:var(--muted);margin-top:8px}
  </style>
</head>
<body>
  <div class="card-reset">
    <h2>Create a new password</h2>
    <p class="lead">Choose a strong password for your account. It must be at least 8 characters long.</p>

    <% if (typeof msg !== 'undefined' && msg) { %>
      <div class="alert alert-info"><%= msg %></div>
    <% } %>

    <form action="/reset-password" method="POST">
      <!-- include email token as needed: -->
      <input type="hidden" name="email" value="<%= email || '' %>">
      <div class="mb-3">
        <label class="form-label">New Password</label>
        <input type="password" name="password" id="pw1" class="form-control" placeholder="Enter new password" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Confirm Password</label>
        <input type="password" name="confirm" id="pw2" class="form-control" placeholder="Confirm new password" required>
      </div>

      <div class="password-guideline">
        Password must be at least 8 characters and include a number.
      </div>

      <button type="submit" class="btn btn-success w-100 mt-3">Reset Password</button>
    </form>

    <a href="/login" class="link-secondary d-block text-center mt-3">Back to Login</a>
  </div>

  <script>
    // client-side basic validation before submit
    const form = document.querySelector('form');
    form.addEventListener('submit', (e) => {
      const p1 = document.getElementById('pw1').value.trim();
      const p2 = document.getElementById('pw2').value.trim();
      if (p1.length < 8 || !/\d/.test(p1)) {
        e.preventDefault();
        alert('Password must be at least 8 characters and include a number.');
        return;
      }
      if (p1 !== p2) {
        e.preventDefault();
        alert('Passwords do not match.');
        return;
      }
    });
  </script>
</body>
</html>
